@page "/"
@attribute [Authorize]
@rendermode InteractiveServer

<PageTitle>Beranda — ModeOn</PageTitle>

<h4 class="fw-bold mb-1">Selamat datang di ModeOn</h4>
<p class="text-muted">Sistem manajemen dealer penjualan sepeda motor.</p>

<AuthorizeView>
    <Authorized>
        <p class="small text-muted mt-3">
            Masuk sebagai <strong>@context.User.Identity?.Name</strong>.
        </p>
    </Authorized>
</AuthorizeView>
